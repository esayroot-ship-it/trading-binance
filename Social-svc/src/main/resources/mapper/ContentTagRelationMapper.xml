<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.socialsvc.mapper.ContentTagRelationMapper">

    <sql id="Base_Column_List">id, target_id, target_type, symbol, create_time</sql>

    <select id="selectById" resultType="org.example.common.entity.ContentTagRelation">
        SELECT <include refid="Base_Column_List"/>
        FROM content_tag_relation
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="org.example.common.entity.ContentTagRelation">
        SELECT <include refid="Base_Column_List"/>
        FROM content_tag_relation
    </select>

    <insert id="insert" parameterType="org.example.common.entity.ContentTagRelation">
        INSERT INTO content_tag_relation (target_id, target_type, symbol, create_time)
        VALUES (#{targetId}, #{targetType}, #{symbol}, #{createTime})
    </insert>

    <update id="updateById" parameterType="org.example.common.entity.ContentTagRelation">
        UPDATE content_tag_relation
        SET
            target_id = #{targetId},
            target_type = #{targetType},
            symbol = #{symbol},
            create_time = #{createTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM content_tag_relation
        WHERE id = #{id}
    </delete>

    <delete id="deleteByTarget">
        DELETE FROM content_tag_relation
        WHERE target_type = #{targetType}
          AND target_id = #{targetId}
    </delete>

    <select id="selectSymbolsByTarget" resultType="java.lang.String">
        SELECT symbol
        FROM content_tag_relation
        WHERE target_type = #{targetType}
          AND target_id = #{targetId}
        ORDER BY id ASC
    </select>

</mapper>
