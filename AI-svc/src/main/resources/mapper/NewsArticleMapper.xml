<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.aisvc.mapper.NewsArticleMapper">

    <sql id="Base_Column_List">id, category_id, title, summary, ai_summary, ai_sentiment, ai_status, content, source, image_url, status, publish_time, create_time</sql>

    <select id="selectVisibleById" resultType="org.example.common.entity.NewsArticle">
        SELECT <include refid="Base_Column_List"/>
        FROM news_article
        WHERE id = #{id}
          AND status = 1
    </select>

</mapper>
